welcome to this basic video about about artificial neural networks networks a neural network consists of input nodes hidden layer a and output nodes nodes for example these input nodes may nodes may represent three measurements that we do we do to determine if someone has prostate has prostate cancer or not not suppose that the person enters the enters the hospital hospital hospital we check the age check the age and the concentration of the prostate the prostate specific antigen from a blood sample blood sample and collect a score between 1 and 5 from and 5 from an MRI scan of the prostate the prostate then we plug in the corresponding values in the corresponding values in the input nodes the input nodes and use the network to tell if the the person not not to understand how neural Nets work we neural Nets work we will here have a look at the super super simple example simple example simple example at the end of this video we'll try to we'll try to understand what happens if you include a if you include a hidden layer hidden layer hidden layer a look at some simple R code if you like to reproduce the first example shown in first example shown in this video this video this video we will here see how to train a neural train a neural network to predict if someone has someone has prostate cancer based on the PSA level on the PSA level our training data consists of seven of seven patients that we know have prostate have prostate cancer cancer cancer and seven individuals that we know are that we know are healthy based on a blood sample blood sample we have determined the concentration of concentration of PSA or the prostate specific antigen in prostate specific antigen in the blood of these individuals note that this data has been simulated data has been simulated for the purpose of this video this video we'll here use the simplest possible simplest possible neural network model model since we only have one measured variable have one measured variable the PSA concentration the network will network will only have just one input node input node note that we here do not have any hidden have any hidden layer layer layer because we like to use the simplest to use the simplest possible Network possible Network possible Network the network has two output nodes because two output nodes because we here like to use the network to prostate cancer predict if someone has or not or not this is called a bias which is used to is used to modify the activation function the activation function there are several different activation functions to select between to select between we will here use the sigmoid function use the sigmoid function which is the same function that is used used in logistic regression regression this will later allow us to compare the the neural network with logistic regression let's make a plot of this data plot of this data like this like this like this where these points represent the cancer the cancer patients that are hair coded as once hair coded as once whereas these points represents the healthy individuals that are coded as coded as zeros zeros zeros for example this healthy individual has individual has a PSA level of 2.5 whereas this cancer patient has a PSA patient 2.1 training a neural network means that we that we find the optimal values of the weights values of the weights and the weight associated with the with the buyers buyers buyers so that the activation function in this activation function in this case case case generates a sigmoid curve that can be curve that can be used to predict the outcome in an the outcome in an optimal way optimal way optimal way note that the logistic activation function generates a value between 0 and value between 0 and 1. 1. 1. and that e is the Euler's number is the Euler's number if you train this simple Network we will will obtain the following weights the following weights let's use this network to see if this this healthy person is correctly predicted as predicted as being healthy by the network we plug in its PSA level of 2.0 in the input node input node input node to calculate the probability that the that the person has cancer cancer we use the weights associated with associated with output node output node output node in this equation equation where W cro is the bias weight is the bias weight and this is the way for the input signal the input signal whereas this is the value of the input input node node node weight that is only have one weight that is associated with the output node n here equal to 1. equal to 1. let's plug in the weights in the the weights in the equation equation equation and node input the input node and do the math do the math we now plug in this value in the the logistic activation function function and calculate and calculate and calculate this value is the value for the output output node cancer node cancer node cancer which can in this case be seen as the seen as the probability that the person has prostate has prostate cancer cancer cancer let's place this value here we'll now calculate the value of this the value of this node where corresponding corresponding values in the equation in the equation the math let's place the corresponding value here value here which can be seen as the probability the person is healthy that the person is healthy we can now use some can of threshold to of threshold to determine if you should classify the classify the individual to have prostate cancer or cancer or not based on these two values two values a common kind of value to use in binary use in binary classification is 0.5 because it is the because it is the based on this cutoff value the network cutoff value the network will predict that the person with the the person with the PHA level of 2 is healthy or cost output or cost output value associated with the healthy class healthy class is greater than 0.5 greater than 0.5 note that this value is exactly the same same value value value as if we would draw a vertical line from vertical line from 2 to the logistic curve and then the horizontal line from the horizontal line from the curve to the y-axis to the y-axis the height of the activation function is the activation function is therefore equal to 0.438 this value is then simply just 1 minus just 1 minus 0.438 this value is equal to the height of the the height of the flipped curve flipped curve flipped curve because the slope of the curve should be be negative if you go this way in the the network if you have seen my previous videos previous videos about logistic regression you will note will note that such a model reproduced exact same model reproduced exact same results results results the estimated parameters of the logistic the logistic regression model regression model regression model are identical to the weights in this the weights in this simple neural network network P0 or The Intercept corresponds to the Intercept corresponds to the bias bias bias whereas B1 corresponds to the weight output node for node for prostate cancer prostate cancer prostate cancer if we plug in this equation into the the activation function activation function activation function we will obtain the exact same that is used in logistic regression logistic regression we can therefore conclude that this that this simple neural network using the logistic the logistic function is identical to logistic regression let's see how well this neural network well this neural network can predict the class of our training class of our training data data data this is the data that we used to train used to train the network and this is the values this is the values generated by the output node for cancer output node for cancer which can be obtained if you plug in in these values in the equation and the weights weights and then compute the corresponding values by the activation function for example this is the value that we we calculated by hand previously based on of 2.0 the PSA level of 2.0 which corresponds of the of activation function we can now use the output values and the values and the cutoff values 0.5 to see how well well the model predicts the training data since these individuals had values greater than 0.5 they are predicted to they are predicted to have prostate cancer cancer since you know that these individuals these individuals actually have prostate cancer we know we know that the neural network has made the network has made the correct predictions for these predictions for these individuals individuals individuals however the network incorrectly predicts that this cancer patient is healthy is healthy these individuals have output values that are less than 0.5 which means that they are predicted to they are predicted to be healthy since we know that these we know that these individuals are healthy we know that know that they have been correctly classified by classified by the network the network the network this healthy person is incorrectly classified as having prostate cancer because it has a relatively high PSA a relatively high PSA level level level in total the network makes 12 correct network makes 12 correct predictions out of 14 possible which gives an accuracy of about 86 accuracy of about 86 percent percent percent however to get the fair estimate of how how well the network would perform on new would perform on new data data data we should use a test data set or or cross-validation as we have done in all have done in all other machine learning methods that we that we have discussed so far discussed so far watch the video about validation to see about validation to see how to evaluate the classifier by the the classifier by the holdout method or by cross-validation once they have established a neural a neural network that has been trained on some been trained on some data we can use it for prediction use it for prediction suppose that we like to test if this this or or not from a blood sample a blood sample we measure the PSA concentration to 1.75 PSA concentration to 1.75 nanograms per ml ml we plug in the value in the network network we can then compute the values of the the output nodes output nodes output nodes we therefore plug in the value of the the input node here here the weight of the buyers and this weight here this weight here and do the map do the map then we the value in this value in activation function and do the math again 1 minus 0.281 is 0.719 since this value is larger than the is larger than the cutoff value of 0.5 the network predicts 0.5 the network predicts we'll now try to understand where the to understand where the values for the weights come from come from in this example these are the optimal these are the optimal values for the weights for the weights whereas for values optimal values for the bias weights weights the weights are optimized by using some optimized by using some sort of cost function of cost function usually the maximum likelihood method for binary classification that we have we have covered in the videos about logistic about logistic regression regression regression the following weights were optimized by optimized by the negative log likelihood function we can also use the method of ordinary ordinary squares I will here explain the concept here explain the concept of ordinary squares because it is a bit bit simpler to understand understand regression logistic regression I show how to calculate the log to calculate the log likelihood likelihood likelihood and in another video I share the video I share the difference between orderly squares and the maximum likelihood method based on based on the simple linear model simple linear model when we use the method of lead squares squares we try to minimize this function that minimize this function that computes the sum of the squared errors of the squared errors these are the Y values of the Y values of the observations which are in this example this example equal to zero if it is a healthy person a healthy person and one if it is a patient with prostate patient with prostate cancer cancer cancer y hat reactivation of the value of the reactivation function that can go between 0 and 1. 1. this difference is called a residual or called a residual or an error the seen as be seen as the distance between observations and the Curve Curve Curve the Y value of this data point is zero point is zero curve on the PSA the value of the curve on the PSA level is equal to 2.5 is 0.75 to 2.5 is 0.75 which can be calculated like this based calculated like this based on the weights we previously estimated previously estimated this results in a residual of negative a residual of negative 0.75 0.75 0.75 we then Square this residual we then do the same calculations for the the next data point and so forth so forth then we sum all the square residuals all the square residuals which here results in a value of 1.78 1.78 suppose that we now change the value of of the bias from negative 5.754 to negative 8. that will move the curve a bit to the a bit to the right right right which will increase the sum of squared the sum of squared errors because the data points are now data points are now much farther away from the Curve the Curve let's paste the point here which here which represents that the sum of squared of squared residuals or errors is 2.88 and the bias bias is set to negative 8. when the 5.754 to negative set to negative 5.754 errors was equal of the squared errors was equal to 1.78 to 1.78 to 1.78 if you set the bias to negative 3 3 we'll to the left bit the left which will result in that the sum of the is equal to 3.62 squared errors if we would try many different values of of the buyer's weight weight we will be able to generate a curve that a curve that shows at errors the squared of the squared errors changes we will change the value or the bias weight associated with the bias the method of least squares finds the least squares finds the value of the weight that results results in the lowest possible sum of squared errors squared errors this explains whether weight of the bias weight of the bias is equal to about negative 5.8 in this this example because that value results in a value results in a curve that is as close as possible to to the data points which we'll result in which we'll result in that the network predicts the prostate the prostate cancer in the best way to find that the lowest sum of squared results in of squared errors errors errors we need to start with an initial guess guess suppose that we initially guess the guess the value or the weight to negative three weight to negative three then we use an algorithm such as the the gradient descent where we step by smooth we step by smooth along the direction of the steepest the steepest descent descent descent like this If instead initially guess the value of value of the bias to negative 9. the gradient descent method will instead will instead increase until weight the weight until it finds the value that minimizes the value that minimizes errors one problem occurs when we had a more more complicated Network because the error function for such a network would then a network would then have several local minimals several local minimals this means that if initially gets the if initially gets the negative 9 bias to negative 9 a method will move down to the local local minimum and Report negative 8 as the negative 8 as the best value best value best value which will result in a bad fit to bad fit to the data data data if you initially guess the value to guess the value to negative 6.5 negative 6.5 negative 6.5 or negative 3.5 3.5 then we will reach the global minimum reach the global minimum which results in the best fit best fit it is therefore important that we try important that we try many initial guesses or the weights the weights during the learning process to find a process to find a network that can predict the data in the the best way best way best way most software tools Generate random which means that you might get different you might get different results every time you train your train your network network network we can make a similar error curve if we curve if we change this weight weight when we change this weight the Curve this weight the Curve will change its slope change its slope when we estimate to weight simultaneously the error function will correspond to surface in a three-dimensional plot like this plot like this the combined values of the two weights of the two weights that result in the minimum value of this this function will result in the best fit to to the data the data the data the purpose to understand the purpose of using a hidden layer in neural hidden layer in neural networks networks networks to show the beauty of the Hidden layers layers suppose that we have the following data have the following data where one has measured a certain protein measured a certain protein in blood samples from 10 cancer patients from 10 cancer patients and five Health individuals five Health individuals people who are healthy have an have an intermediate level of this protein in protein in their blood their blood their blood or as people with prostate cancer either with prostate cancer either have a low level a low level or a high or a high level if you would try to fit a logistic logistic regimal would trade a neural network neural network with a logistic activation function without a hidden layer a hidden layer maybe failed to make a good prediction make a good prediction of the training data because an s-shaped data because an s-shaped curve is simply not useful for this type type of data of data of data these cancer patients will incorrectly be predicted to be healthy by them all all and if you flip the curve these patients patients will instead incorrectly be predicted to predicted to be healthy be healthy be healthy the car might also look like this also look like this where all the healthy individuals will individuals will now be incorrectly predicted to have to have cancer what we want is a car that looks like that looks like this this this this is where the hidden layer comes in in because a neural network with one or network with one or several hidden layers can generate cool generate cool curves like this this we'll here extend our previous network previous network with one hidden layer one hidden layer if you train this network on the following data the following data will obtain the following values or the following values or the weights weights weights node if you try to compute the neural neural network on the same data you will end up will end up with different values compared to the to the one shown here because you will probably because you will probably end up on a different local minimum to to understand the calculations behind this it predicts see how it predicts a person with a value of 3 or protein X the value of this node which results in a value that is a value that is approximately equal to one equal to one let's set to 1. this node value of this node to 1. the corresponding value of this node is of this node is 0.869 node is node is calculated based on on the bias the bias the bias and the sum of the weight multiplied by by the first node in the hidden layer in the hidden layer and the way it multiplied by the second second node in Hidden layer in Hidden layer similarly the value of the second output second output node is approximately equal to zero to zero if node is 3. input value of the input node is 3. the person predict cancer let's focus on how this output node is output node is calculated calculated calculated this is how this node was and this is how the values of the nodes of the nodes in a hidden layer were calculated were calculated let's substitute X1 and x2 in the and x2 this and in equation this equation in activation function here here so function following the following function that can compute the value of the output output node based on any input value input value let's move the function up here up here if you the value of the value of the function based on a range of different a range of different values of X between 0 and 4. we will be with this shape generate a curve with this shape which predicts with 100 accuracy because all individuals with prostate cancer will get a value of approximately a value of approximately one and will therefore be correctly be correctly whereas will will get a predicted value of approximately of approximately zero they are that result in that they are correctly predicted to be healthy similarly the corresponding function of this output node node will generate the opposite curve this is the magical stuff behind neural magical stuff behind neural networks they can help us to generate help us to generate non-linear functions that can fit to fit to almost any data that seems impossible do with standard statistical methods we'll now discuss a few similarities and a few similarities and differences between neural networks and standard statistical models such as the as the logistic regression model model the input in neural Nets is usually neural Nets is usually called a predictor or explanatory variable in logistic regression in logistic regression the output is usually is usually called the response variable in logistic in weights in neural networks are the are usually called coefficients or called coefficients or parameters in logistic regression in logistic regression The Intercept in regression corresponds to the bias bias we usually say that we train the neural neural network whereas the corresponding thing in regression is called that we fit them them all to the data would that be estimate estimate the parameters in the model the parameters in regression are estimated by minimizing some error function such as the sum of squares in in regression regression regression whereas the corresponding thing is called back propagation in neural Nets neural Nets because we randomly assign the weights the weights with some numbers numbers then we see how well the network how well the network predicts the training data the training data then we go back to update the values values so that we improve the prediction the prediction this is iterated until we reach some until we reach some convergence where the network can no can no longer improve the predictions based on based on the training data data the parameters in regression usually have a meaning that we can interpret that we can interpret such as the odds ratio in the district district values of the ways in the whereas the the ways in the neural network usually have no meaning no meaning rather that they're used to make a good good prediction prediction prediction in regression we usually compute P compute P values that are associated with the with the parameters which require that to fulfill to fulfill a set of assumptions set of assumptions since we use the estimated parameters to the estimated parameters to interpret the regression model we must we must find the global minimum of the error minimum of the error function function function whereas this is not crucial in neural not crucial in neural networks because the network might predict the outcome just fine even fine even though it has stopped on a local minimum finally we'll have a look at some basic at some basic to reproduce the if you like to reproduce the numbers in this video in this video R is a free software tool that you can that you can download from the following website we first plug in the data into an R into an R script script script will he use the neural net package which which you first need to install if you have have not already done that already done that then we load this package and train the neural network where we try to predict cancer versus to predict cancer versus healthy which is level the PSA level which is the only input in this case this case we plug in the training data here the training data here since we in our example like to chain a to chain a neural network without a hidden layer we a hidden layer we set this argument to zero and use the logistic activation function activation function the arrow function is here set across is here set across entropy which in this example is the this example is the same thing as using the negative log using to estimate the likelihood function to estimate the weights we can print the output and plot the network plot the network after after after 754 iterations to optimize the values of optimize the values of the weights in order to minimize the order to minimize negative log likelihood function negative log likelihood function we have an error of 10.54 which in this which in this case corresponds to the negative log negative log likelihood value likelihood value likelihood value the smaller this value is the better the the mole fits the data fits the data for example if you like to use the the network for prediction we can use function predict function predict function predict in this example the network will that the person with a PC level of 2 is healthy healthy healthy if you like to compare with the logistic logistic regression model you can run the run the following line if you like to change the error function the error function to you can set errors sum of squared errors you can set this argument to SSE argument to SSE the reason why here use a lower here use a lower threshold for convergence compared to the default value of 0.01 is simply to to get similar values as in logistic in logistic regression but this will be more be more computational expensive computational expensive computational expensive I also recommend that you run a number number of repetitions of repetitions of repetitions in this example we will generate 10 we will generate 10 networks that have been based on based on different initial values of the weights the weights then you can study the output of these these 10 networks and select the one with the the lowest error in future videos a look at networks where the output can result in output can result in several categories compared to the simple binary case we used in this video video we'll also have at networks where is a continuous variable the output and see how the number of hidden nodes nodes affects the output we will also discuss we will also discuss the problem of overfitting problem of overfitting in another video I will cover non-linear I will cover non-linear regression which is highly related to related to neural networks neural networks neural networks this was the end of this basic video video about artificial neural networks thanks for watching